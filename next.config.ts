import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Untuk Capacitor, uncomment setelah desain selesai:
  // output: 'export',
  // trailingSlash: true,
  // images: { unoptimized: true },
}

export default nextConfig
